# 北极星出行攻略 · 完整版安装说明

> 装好之后，对 WorkBuddy 说一句「**我要出去玩，帮我整理旅行手册**」，
> 它会自动打开问卷 → 你填完 → 复制提示词发回去 → 得到一份可分享、可勾选的网页版攻略。

这个包是**完整版**：技能和它依赖的项目都在里面，解压即用，不需要额外准备任何东西。

---

## 一、包里有什么

```
北极星出行攻略-完整版/
├── 安装说明.md                     ← 你正在看的这份
├── 技能/
│   └── polaris-trip-handbook/      ← 技能本体（约 40 KB）
│       ├── SKILL.md
│       ├── references/             3 份细则（陷阱 / 内容规则 / 字段映射）
│       └── scripts/open-questionnaire.py
└── 项目/
    └── workbuddy迁移包/             ← 生成引擎（约 0.3 MB，放到任意位置）
        ├── scripts/                9 个 Python 脚本
        ├── assets/                 页面模板 + 问卷页 + 交通价表
        ├── docs/                   需求 / 契约 / 路线文档
        ├── seed/                   数据 schema
        └── trips/                  行程产物目录（本包只含一个离线渲染 demo）
```

**为什么是两个部分？** 技能是**说明书**（怎么一步步做），项目是**发动机**（真正干活的代码）。
技能本身不含生成引擎 —— 所以两个都要装，缺一不可。

> 机器名 `polaris-trip-handbook` 是「北极星 = Polaris」。WorkBuddy 要求技能机器名只能用小写字母、
> 数字和连字符，中文名无法通过校验，所以中文名「**北极星出行攻略**」放在技能的标题和描述里。
> **不影响使用**，你照样叫它北极星。

---

## 二、前置条件

只有一个：**Python 3.10 或更高版本**。

- 项目**只用标准库**，不需要 `pip install` 任何东西
- 不需要 Node（除非你要跑问卷自测，那是开发时才用的）
- 不需要联网（攻略生成用的是本地规则引擎 + 会话内模型）
- 不需要任何 API Key

检查 Python：

```bash
python --version
```

---

## 三、安装（三步）

### 第 1 步：装技能

把 `技能/polaris-trip-handbook` 整个文件夹，复制到 WorkBuddy 的技能目录：

```
Windows：     C:\Users\<你的用户名>\.workbuddy-ai\skills\
macOS/Linux： ~/.workbuddy-ai/skills/
```

装完后应该是这个层级：

```
.workbuddy-ai/skills/polaris-trip-handbook/SKILL.md     ← 对
```

⚠️ **最常见的错误**：多套了一层文件夹，变成
`skills/polaris-trip-handbook/polaris-trip-handbook/SKILL.md` —— 这样不会被识别。

> 也可以偷懒：把 `技能/polaris-trip-handbook` 拖进 WorkBuddy 对话，说
> 「把这个技能安装到用户级技能目录」，让它自己装。

### 第 2 步：放项目

把 `项目/workbuddy迁移包` 整个文件夹，放到**你喜欢的任意位置**。比如：

```
D:\tools\workbuddy迁移包\
```

**不要**把它放进技能目录里 —— 两者是独立的。

> **也可以就地不动**：直接解压到哪就用哪，把 `北极星出行攻略-完整版` 整个文件夹当成工作区打开，
> 技能会自动搜到 `项目/workbuddy迁移包`（它按 5 层深度搜，够用）。
> 这也是最省事的方式 —— 不用搬、不用记路径。
>
> 小技巧：如果你平时就在某个工作区里干活，把项目放在那个工作区下，技能第一次就能自动搜到，
> 连第 3 步都省了。

### 第 3 步：告诉它项目在哪（只做一次）

新开一个对话，说：

> 我要出去玩，项目在 `D:\tools\workbuddy迁移包`

技能会自己找到项目，并**把路径记进技能目录下的 `.project-path` 文件**。以后就不用再说了。

---

## 四、验证装好了没

**方法 1（看文件）**：

```
C:\Users\<用户名>\.workbuddy-ai\skills\polaris-trip-handbook\SKILL.md
```

存在即装好。

**方法 2（直接试）**：新开对话说

> 我要出去玩

如果它开始帮你打开问卷页，就成了。

---

## 五、怎么用（完整流程）

| 步 | 你做什么 | 会发生什么 |
|---|---|---|
| 1 | 说「**我要出去玩，帮我整理旅行手册**」 | 自动起本地服务 + **弹出问卷页**，然后等你 |
| 2 | 在问卷页填：出发地、目的地、日期、人数、想怎么玩 | 晚数自动算，不用你填 |
| 3 | 点「**生成开工提示词**」→「复制提示词」 | 拿到一段中文提示词 |
| 4 | 把那段文字粘回对话发出去 | 它会先跟你**核对疑点**（班次真假、时段矛盾等），再开跑 |
| 5 | 等一会儿 | 拿到 `handbook.html`，可打开、可分享、可勾选 |

**第 3 步的小技巧**：问卷页上还有个「**下载 brief.json**」按钮 ——
把那个文件直接发回去比复制文本更准，少一层解析。

---

## 六、会触发这个技能的说法

- 「我要出去玩」「帮我整理旅行手册」
- 「帮我生成攻略」「规划个 X 天的行程」「想去成都玩几天」
- 直接粘一段以「请用 trip-handbook-maker 为我生成」开头的提示词
- 「做行程手册」「把攻略做成网页」「更新一下之前那份攻略」

---

## 七、出问题怎么办

| 现象 | 原因 | 怎么办 |
|---|---|---|
| 说「我要出去玩」没反应 | 技能没装上 / 装错层级 | 回到第三节第 1 步，注意别多套一层文件夹 |
| 提示「找不到项目」 | 项目没放 / 路径没告诉它 | 告诉它项目路径；它会记住 |
| 项目明明就在包里，却说找不到 | 老版本技能只搜 3 层深，包内项目在第 4 层 | 用本包里的技能（已修成搜 6 层）；或直接把路径告诉它 |
| 换机器后又说找不到项目 | `.project-path` 缓存里是旧路径 | 删掉技能目录下的 `.project-path`，重新告诉它一次 |
| 问卷页打不开 | 本地服务没起来 | 让它重跑一次；或手动访问它给出的 `http://127.0.0.1:端口/...` 链接 |
| 提示缺 `open-questionnaire.py` | 项目版本较旧 | 把技能 `scripts/` 下那个同名文件复制到项目的 `scripts/` 里 |
| 比价说「缺数据」 | 价表里没这条线路的真实票价 | 这是**正常行为**（设计上不猜价格）。让它联网查了填进 `assets/transport-fares.json` |

---

## 八、卸载

删掉两处即可：

```
C:\Users\<用户名>\.workbuddy-ai\skills\polaris-trip-handbook\    ← 技能
<你放项目的位置>\workbuddy迁移包\                                  ← 项目
```

---

## 附一、关于里面的示例行程

`项目/workbuddy迁移包/trips/` 下只有一个**离线渲染 demo**（`sy-yj-cc-demo/handbook.html`，
由 `seed/shenyang-yanji-changchun.sample.json` 渲染而来），用来验证渲染链路是否正常。

本包**不附带任何真实行程**：新行程在你填完问卷后现场生成，产物同样落在 `trips/<slug>/` 下。

## 附二、安全说明

本包**不含任何可执行的风险操作**：

- 技能部分：4 个 `.md` 文档 + 1 个 Python 脚本。该脚本**仅用标准库**，
  只做三件事 —— 在 `127.0.0.1` 起本地静态服务、用浏览器打开本地问卷页、常驻等待。
  **不联网上传、不读凭证、不删文件、不写注册表。** 可以自己打开核对，约 90 行。
- 项目部分：9 个 Python 脚本，同样**仅用标准库**，全部是本地文件读写 + 规则计算。
  唯一的外部访问是**可选的**模型 API（走 `%USERPROFILE%\.trip-handbook\config.json` 里的 Key）——
  **不配 Key 也能完整使用**（无 Key 模式由会话内模型直接产出内容）。
- 没有任何脚本会向外部发送你的数据。

## 附三、技术细节（想知道原理的话）

- **技能 = 编排器**：它不含生成逻辑，只负责定位项目 → 按正确顺序调脚本 → 在关键节点做判断 → 规避已知陷阱
- **项目 = 规则引擎 + 渲染器**：先用规则搭 0 token 的骨架（日期、城市晚数、交通段），
  再补内容，最后渲染成**单文件 HTML**（CSS/JS 全内联，无框架、无 CDN、无构建）
- **天数口径**：`days` = 自然日数（含返程日）；`nights` = 各城晚数之和 = `days - 1`，必须严格差 1
- **不编造**：营业时间、价格、评分、预约规则、车次航班号一律不编 ——
  查不到就留空或用「参考区间」+「以官方公告为准」
- **交通比价**：`score = 票面×人数 + 接驳×人数 + 耗时×时间价值×人数×时间权重 − 舒适补偿`，越低越优；
  三档口径 `thrifty`（省钱）/ `balanced`（均衡）/ `comfort`（舒适）
